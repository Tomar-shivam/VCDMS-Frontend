let handleVideoClick = (setContent) => {
  setContent("video");
};

let handleAudioClick = (setContent) => {
  setContent("audio");
};

let handleTSClick = (setContent) => {
  setContent("ts");
};

let handleFirmwareUpdateClick = (setContent) => {
  setContent("firmware");
};

let handleSystemClick = (setContent) => {
  setContent("system");
};

let handlePcvisionClick = (setContent) => {
  setContent("pcvision");
};
export {
  handleVideoClick,
  handleAudioClick,
  handleTSClick,
  handleSystemClick,
  handleFirmwareUpdateClick,
  handlePcvisionClick,
};
