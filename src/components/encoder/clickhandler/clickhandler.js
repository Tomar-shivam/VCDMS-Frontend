let handleVideoClick = (setContent) => {
  setContent('video')
}

let handleAudioClick = (setContent) => {
  setContent('audio')
}

let handleTSClick = (setContent) => {
  setContent('ts')
}

let handleNetworkClick = (setContent) => {
  setContent('network')
}

let handleSystemClick = (setContent) => {
  setContent('system')
}

let handlePresetClick = (setContent) => {
  setContent('preset')
}

let handleDashboardClick = (setContent) => {
  setContent('dashboard')
}
let handlePcVisonClick = (setContent) => {
  setContent('pcVision')
}

export {
  handleVideoClick,
  handleAudioClick,
  handleTSClick,
  handleNetworkClick,
  handleSystemClick,
  handlePresetClick,
  handleDashboardClick,
  handlePcVisonClick,
}
